import { Router } from 'express';

const chatRouter= Router();

// chatRouter.post('/new-chat', createChat);
// chatRouter.get('/fetch-chat/:id', fetchChat);
// chatRouter.patch('/update-chat/:id', updateChat);

export default chatRouter;